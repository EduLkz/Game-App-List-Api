{
  "name": "game-list-api",
  "version": "1.0.0",
  "description": "api for game-list-app",
  "main": "./dist/index.js",
  "scripts": {
    "start": "node ./",
    "build": "npx tsc -p .",
    "dev": "npx ts-node-dev --respawn --transpile-only --ignore-watch node_modules --no-notify src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "game-list",
    "node",
    "api"
  ],
  "author": "Edu Rocha",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/http-status-codes": "^1.2.0",
    "@types/pg": "^8.6.5",
    "http-status-code": "^2.1.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "pg": "^8.8.0"
  }
}
